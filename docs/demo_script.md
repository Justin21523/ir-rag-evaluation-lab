# Demo Script

1. Run `make sample-data`.
2. Run `make index`.
3. Run `make evaluate`.
4. Start `make api` and `make frontend`.
5. Open Query Evaluator and compare BM25, dense, hybrid, and rerank.
6. Open RAG Citation Checker and inspect evidence, citations, and checklist.
7. Open Bad Cases and discuss failure taxonomy.
